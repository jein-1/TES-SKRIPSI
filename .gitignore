# ── Dependencies ──────────────────────────────────────────────
node_modules/

# ── Build outputs (tidak perlu di-commit, Railway build sendiri)
dist/
dist-admin/

# ── Env files (JANGAN commit — simpan di Railway env variables)
.env
.env.local
.env.*.local
# .env.admin boleh di-commit karena hanya VITE_ADMIN_APP=true (tidak rahasia)

# ── Backup files ───────────────────────────────────────────────
*.bak
*.backup
*.tmp

# ── Editor & OS ───────────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db

# ── Logs ───────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-error.log*

# ── Android (tidak perlu semua binary di git) ─────────────────
android/.gradle/
android/build/
android/app/build/
android-admin/.gradle/
android-admin/build/
android-admin/app/build/
*.apk
*.aab

# ── Android SOURCE folders — build lokal, tidak di-push GitHub ─
# APK di-sync lokal saja, codenya ada di src/ (web unified)
android/
android-admin/
